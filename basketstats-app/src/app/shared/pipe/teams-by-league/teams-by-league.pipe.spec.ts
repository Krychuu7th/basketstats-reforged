import { TeamsByLeaguePipe } from './teams-by-league.pipe';

describe('TeamsByLeaguePipe', () => {
  it('create an instance', () => {
    const pipe = new TeamsByLeaguePipe();
    expect(pipe).toBeTruthy();
  });
});
