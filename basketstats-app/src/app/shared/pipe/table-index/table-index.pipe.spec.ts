import { TableIndexPipe } from './table-index.pipe';

describe('TableIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new TableIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
